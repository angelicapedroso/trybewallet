import React, { Component } from 'react';

export default class BtnAdd extends Component {
  render() {
    return (
      <button
        type="submit"
      >
        Adicionar despesa
      </button>
    );
  }
}
